import React from 'react';

import UrlForm from './UrlForm';

class App extends React.Component {
   render() {
      return (
         <div>
            <UrlForm />
         </div>
      );
   }
}

export default App;
